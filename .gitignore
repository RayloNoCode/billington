# Secrets — never commit these
.env
raylo-service-account.json

# Dependencies
node_modules/

# PM2 logs
*.log
.pm2/

# Mac junk
.DS_Store

# Knowledge files with personal instructions (back these up separately)
knowledge/hargo-instructions.txt

# Runtime state (ephemeral on cloud deploys)
reminders.json
aryza-tickets.json

# Generated GCP service account file (written from env var at startup)
gcp-sa.json

# Personal Claude-session kickoff doc (not for the shared repo)
BILLINGTON_CONTEXT.md
# Runtime balance-history caches (durable copy lives in GCS)
stannp-balance-history.json
gocardless-balance-history.json
